import SwiftUI

struct MeetingPrepContextPanel: View {
    let meeting: Meeting
    var body: some View {
        Text("Contexte — placeholder (Task P8)")
            .font(.caption)
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

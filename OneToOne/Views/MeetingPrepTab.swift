import SwiftUI

struct MeetingPrepTab: View {
    let meeting: Meeting
    var body: some View {
        Text("Préparation — placeholder (Task P6)")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
